#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
   printf("%d",a+b);
   printf("\n%d",abs(a-b)); 
    return 0;
}
